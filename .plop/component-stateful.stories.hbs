import { storiesOf } from '@storybook/react';
import { WithNotes } from '@storybook/addon-notes';
import { text } from '@storybook/addon-knobs';
import React from 'react';
import { {{pascalCase name}} } from './{{kebabCase name}}.component';

storiesOf('{{pascalCase name}}', module)
  .add('Basic', () => {
    return (<WithNotes notes={''}><{{pascalCase name}} name={'{{pascalCase name}}'} /></WithNotes>);
  })
  .add('Configurable', () => {
    return (<{{pascalCase name}} name={text('Name', '{{kebabCase name}}')} />);
  });
