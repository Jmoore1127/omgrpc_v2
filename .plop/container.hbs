import { connect } from 'react-redux';

import { {{pascalCase name}} } from './{{kebabCase name}}/{{kebabCase name}}.component';

export const {{pascalCase name}}Container = connect(
  (state) => ({

  }),
  () => ({

  })
)({{pascalCase name}});
