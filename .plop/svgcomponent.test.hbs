import { shallow } from 'enzyme';
import { Raw{{pascalCase name}} as {{pascalCase name}} } from './{{kebabCase name}}.component';

describe('{{pascalCase name}}', () => {
  it('returns a function', () => {
    const actual = typeof {{pascalCase name}};
    const expected = 'function';
    expect(actual).to.be.equal(expected);
  });

  it('renders an svg', () => {
    const wrapper = shallow(<{{pascalCase name}} />);
    expect(wrapper.find('svg')).to.have.length(1);
  });

  it('passes on props', () => {
    const wrapper = shallow(<{{pascalCase name}} newProp={1} />);
    expect(wrapper.find('svg').props()).to.include({ newProp: 1 });
  });
});
